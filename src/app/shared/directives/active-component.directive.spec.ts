import { ActiveComponentDirective } from './active-component.directive';

describe('ActiveComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveComponentDirective();
    expect(directive).toBeTruthy();
  });
});
